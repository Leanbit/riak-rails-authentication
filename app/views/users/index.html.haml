= session.inspect
%h1 Listing users

%table
  %tr
    %th Email
    %th Name
    %th Password
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.email
      %td= user.name
      %td= user.password
      %td
        = link_to 'Show', user_path(user.id)
        = link_to 'Destroy', user_path(user.id), :method => :delete, :confirm => 'Are you sure?'

%br

= link_to 'New User', new_user_path
